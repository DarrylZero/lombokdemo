buildscript {
    repositories {
        jcenter()
    }
}
apply plugin: 'java'

repositories {
    jcenter()
}

dependencies {
    testCompile 'junit:junit:4.12'
    compileOnly fileTree(dir: 'libs', include: '*.jar')
}

task buildChecks {
    dependsOn 'build'
}